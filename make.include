BUILD_DIR := /path/to/pLink-SS/build/
INSTALL_DIR := /path/to/pLink-SS/pLink_release/

CXXFLAGS :=-O3 -Wall -fmessage-length=0

OS := $(shell uname -s)
IS_LINUX := $(shell echo $(OS)|grep -i linux)
IS_WINDOWS := $(shell echo $(OS)|grep -i mingw)

STATICDLL := no
